environment:
  matrix:
  - nodejs_version: 4
  - nodejs_version: 5

install:
  - ps: Install-Product node $env:nodejs_version
  - set PATH=%APPDATA%\npm;%PATH%
  - npm install

test_script:
  - npm test

build: off